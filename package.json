{
  "name": "@neargov/discourse-plugin",
  "version": "0.0.1",
  "description": "Discourse plugin for NEAR governance systems",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "peerDependencies": {
    "every-plugin": "^0.3.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@effect/language-service": "^0.41.1",
    "@module-federation/node": "^2.7.17",
    "@rspack/cli": "^1.5.2",
    "@rspack/core": "^1.5.2",
    "@types/node": "^24.6.0",
    "dotenv": "^17.2.2",
    "every-plugin": "^0.3.0",
    "msw": "^2.3.0",
    "sirv": "^3.0.2",
    "vitest": "^3.2.4",
    "zephyr-rspack-plugin": "^0.0.59"
  },
  "scripts": {
    "build": "rspack build && tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.build.json --noEmit",
    "dev": "rspack serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest --config vitest.integration.config.ts",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@near-js/crypto": "^2.3.3",
    "near-sign-verify": "^0.4.3",
    "@orpc/contract": "^1.10.0",
    "@orpc/server": "^1.10.0",
    "effect": "3.18.1",
    "zod": "4.1.5"
  }
}
